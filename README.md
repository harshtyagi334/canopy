# Canopy

Forest-clearance monitoring platform for tracking compensatory afforestation and verifying ecological recovery from satellite data.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env` and fill in any optional provider credentials.
3. Start the app:
   `npm run dev`

## Production build

`npm run build`
